version: '3.1'

services:
  generator:
    image: 127.0.0.1:55000/gen:ci-${GENERATOR_BUILD_NUMBER-14}
    ports:
      - "8080:80"
  mail:
    image: mailhog/mailhog
    ports:
      - "8025:8025"
  integration:
    image: 127.0.0.1:55000/gen:integration-${BUILD_NUMBER-14}
    build: .
    environment:
      - TEAMCITY_PROJECT_NAME